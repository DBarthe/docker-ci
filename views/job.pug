extends layout

block content
  h2 Elassandra-docker CI
  p Release v#{version}
  h3 Console output:
  p(id='status-text') Job waiting
  ul(id='output-list')

block script
    script.
      $(function() {
        app.submit("#{version}", #{publish}, #{latest})
      })
